{
  "scripts": {
    "clean": "rm -rf ./dist",
    "transpile": "tsc --project ./src/tsconfig.json --rootDir ./src/lambda --outDir ./dist",
    "zip": "zip ./dist/category-fetching.zip ./dist/category-fetching/ --recurse-paths --quiet",
    "build": "pnpm clean && pnpm transpile && pnpm zip",
    "push": "pnpm build && terraform -chdir=infra init && terraform -chdir=infra apply -auto-approve"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
